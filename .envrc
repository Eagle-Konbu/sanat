# 1Password SSH Agent
# This is automatically set by devcontainer.json, but can be overridden here if needed
# export SSH_AUTH_SOCK=/tmp/1password-agent.sock

# Git GPG signing with SSH
# These are set in .gitconfig, but can be verified here
# export GPG_TTY=$(tty)

# Add any project-specific environment variables below

# Load local overrides if present (not tracked in git)
if [ -f .envrc.local ]; then
  source_env .envrc.local
fi
